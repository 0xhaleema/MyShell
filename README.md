# MyShell
MyShell is a lightweight Unix shell built from scratch in C using fork, exec, and wait system calls. It supports multi-command pipelines, input/output redirection, background process execution, persistent command history, and !! to repeat the last command.
